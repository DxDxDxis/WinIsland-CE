const assert=require('node:assert/strict'),path=require('node:path');
(async()=>{const asar=await import('@electron/asar'),archive=path.resolve(__dirname,'../release/settings/resources/app.asar'),files=asar.listPackage(archive);assert(!files.some(f=>f.includes('preview-worker')));for(const file of ['dist/transfer-main.js','dist/preload.js','dist/transfer.js']){const text=asar.extractFile(archive,file).toString();assert(!text.includes('transfer:preview'),file);assert(!text.includes('showPreview'),file);}console.log('PASS packaged preview handler, preload API, renderer and worker removed');})().catch(e=>{console.error(e);process.exitCode=1;});
